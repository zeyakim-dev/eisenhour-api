services:
  e2e-test:
    build:
      context: ../..
      dockerfile: docker/test/Dockerfile
    volumes:
      - ./:/app
    environment:
      TEST_PATHS: ${E2E_TEST_PATHS:-tests/e2e}
      ENABLE_COVERAGE: ${ENABLE_COVERAGE:-false}
