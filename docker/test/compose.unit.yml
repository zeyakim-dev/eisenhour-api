services:
  unit-test:
    build:
      context: ../..
      dockerfile: docker/test/Dockerfile
    volumes:
      - ../../:/app
    environment:
      TEST_TARGETS: ${TEST_TARGETS:-tests/unit}
      COV_OPT: ${COV_OPT:-}
    