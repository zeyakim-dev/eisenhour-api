[project]
name = "eisenhour-api"
version = "0.1.0"
description = ""
authors = [
    {name = "zeyakim-dev",email = "zeyakimdev@gmail.com"}
]
readme = "README.md"
requires-python = "3.12.9"
dependencies = [
]

[tool.poetry]
packages = [{include = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
sphinx = "^8.2.3"
pytest = "^8.3.5"
pytest-cov = "^6.0.0"

[tool.poetry.scripts]
main = "src.eisenhour-api.main:main"
